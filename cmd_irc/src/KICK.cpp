void	KICK()
{
	// si l'extraction échoue ou que channel_name ou nickname est vide -> erreur

	// if user to KICK doesn't exist -> error
	
	// if Channel doesn't exist -> error

	// if user is not in Channel -> error

	// if Client is not an operator -> error, can't KICK

	// on affiche aux memrbes de la Channel que untel a été kick
}
