void	PASS(std::string arg)
{
	User & 		user = ; // GET USER
	std::string server_password = arg;

	if (/* User est déja connecté au server*/)
		// écrire erreur à utilisateur
	else if (/* Wrong password */)
	{
		// écrire ERR_PASSWDMISMATCH (464) au Client
		// déconnecter le client
	}
	else
		// set le password du Client

}
