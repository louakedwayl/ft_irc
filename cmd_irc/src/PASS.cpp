void	PASS()
{
	// si pas d'utilisateur connecté, erreur
	
	// si extraction du mot de passe vers stringstream échoue -> erreur

	// check si mot de passe est pas vide, erreur si oui

	// on change le mot de passe du Client
}
