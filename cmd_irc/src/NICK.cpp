void	NICK()
{
	// si le Client n'est pas connecté, erreur

	// si le nickname est vide, erreur

	// si mauvaise syntaxe du nickname, erreur
	// (erreur syntaxe : commence avec un préfixe de Channel, char invalide dans le nickname)

	// si le nickname est déjà pris, erreur

	// on enregistre l'ancien nickname dans une string

	// on change le nickname

	// on affiche que ancien nickname s'appelle maintenant nouveau nickname
}
