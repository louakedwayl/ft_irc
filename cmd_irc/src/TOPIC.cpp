void	TOPIC()
{
	// si extraction de l'input vers stringstream échoue, ou que channel name ou topic est vide -> erreur

	// check if Channel exists, si non -> erreur

	// check if user is in channel, si non -> erreur

	// si Topic est restreint aux operators et que le Client n'en est pas un -> error

	// si il n'y a pas d'arguments -> Client veut seulement voir le Topic
		
		// si le Topic est vide, écrire cela

		// sinon, écrire le Topic
	
	// sinon -> changer le Topic de la Channel et l'écrire à tous ses membres.
}
